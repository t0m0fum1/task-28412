<h3><%= @task.text %></h3>
<p><%= link_to '編集', edit_task_path(@task.id) %></p>
<p><%= link_to '削除', task_path(@task.id), method: :delete %></p>

<% @contents.each do |content| %>
  <p><%= image_tag content.image.variant(resize:'500x500') %></p> 
<% end %>


